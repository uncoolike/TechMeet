@model TechMeet.DATA.EF.ApplicationStatus

@{
    ViewBag.Title = $"{Model.StatusName} | Details";
}
<div class="paper space">
    <h2 class="spaceup">@ViewBag.Title</h2>

    <div>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.StatusName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.StatusName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.StatusDescription)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.StatusDescription)
            </dd>

        </dl>
    </div>
    <p>
        @if (User.IsInRole("Admin"))
        {
            @Html.ActionLink("Edit", "Edit", new { id = Model.ApplicationStatusId }, new { @class = "btn brows-btn admin-btn" })
        }
        <br />
        <br />
        @Html.ActionLink("Back to Main", "Index")
    </p>
</div>